name: Dependency Review

on:
  pull_request:
    branches:
      - main
      - "publish-v*"

permissions:
  contents: read

jobs:
  review:
    name: Dependency Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/dependency-review-action@v5
