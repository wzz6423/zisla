# UI 问题排查报告

生成时间：2026-07-28 07:36（Asia/Shanghai）

## 结论

副屏模拟岛在没有紧凑状态内容时，上一轮的宠物帧计算与此前完全相同；无法将副屏宠物未常驻复现为该坐标改动造成的问题。

“通知显示器”设置仅过滤播放和 AI 活动通知，不控制宠物持久面板。宠物面板由 `OverlayCoordinator` 在所有非交互显示器上创建。

## 验证

- `swift test --filter OverlayCoordinatorTests`：3 个测试通过，0 个失败。
- 覆盖副屏持久面板、交互屏排除和收起后的恢复行为。

## 待确认

需要确认用户所说“常驻显示”对应的具体设置开关，或提供副屏设置与实际显示状态的截图，以区分设置语义和运行时窗口状态。
